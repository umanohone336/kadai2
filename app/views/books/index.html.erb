<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to "Show", book_path(book.id) %></td>
      <td><%= link_to "Edit", edit_book_path(book.id) %></td>
      <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<!--投稿機能-->
<h2>New book</h2>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @book, url: '/books', method: :post do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>
<!--テーブル全体を<table>で囲みます。
1つ1つの行には<tr>を使います。
その中にセル（1つ1つのマス目）を表す<th>や<td>を入れます。
テーブルのヘッダー（見出し）のセルには<th>を使います。
その他のセルには<td>を使います。
セルを横（右）に伸ばしたいときはcolspan属性を使います。
たとえばcolspan="2"とすると、そのセルが2つ分の大きさになります。
tr要素＝「Table Row」の略で「表の行」を作る
th要素＝「Table Header」の略で「表の見出し」を作る
td要素＝「Table Data」の略で「表のデータ」を作る-->